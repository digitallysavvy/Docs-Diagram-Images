flowchart TB
    start("User: Start the App") --> selectChannel("Select a Channel")
    selectChannel --> login("Login via Developer's Authentication System")
    login --> requestToken("Request Agora Token\n(Channel, Role, Type, User ID)")
    requestToken --> generateToken["Generate Token\n(Integrated Agora Library)"]
    generateToken --> returnToken("Return Token to App")
    returnToken --> useToken("Use Token to Join Channel")
    useToken --> validateToken("Validate Token\n(SD-RTN by Agora)")
    validateToken --> addedToChannel("User Added to Channel")
    addedToChannel --> final("Trigger Callback to Finalize Joining")
    
    classDef leftSide fill:#f96,stroke:#333,stroke-width:2px;
    classDef rightSide fill:#9f6,stroke:#333,stroke-width:2px;
    class start,selectChannel,login,requestToken,returnToken,useToken leftSide
    class generateToken,validateToken,addedToChannel final rightSide